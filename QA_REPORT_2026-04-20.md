# CalCheq Production QA Report
**Date:** 20 April 2026
**Tester:** Nate (nfish82@hotmail.com)
**Environment:** https://calcheq.com (production) — Railway-hosted, Supabase Postgres
**Account used for testing:** nfish82@hotmail.com / site "Calcheq" / role admin (pilot, trial ends 19 May 2026)
**Test data:** all 7 QA instruments soft-deleted (status = decommissioned). 3 attached calibrations retained against decommissioned instrument (no DELETE endpoint exposed — see minor bug). Test document hard-deleted.

**Fix session:** 20–21 April 2026 — fixes applied by Claude Code, committed to `main`, deployed via Railway auto-deploy.

---

## Fix Status Summary

| ID | Severity | Status | Notes |
|----|----------|--------|-------|
| CRITICAL-1 | Critical | ✅ Fixed | Composite constraint applied to DB + ORM |
| CRITICAL-2 | Critical | ✅ Fixed | Approval guard + DB recompute run |
| CRITICAL-3 | Critical | ✅ Fixed | CanonicalManager in App.jsx |
| Major-1 | Major | ✅ Fixed | POST-sign-in cross-check against /api/auth/me; signs out + shows error if site mismatch |
| Major-2 | Major | ✅ Fixed | useDashboard depends on siteName/isDemoMode; guarded with `if (!site) return` |
| Major-3 | Major | ✅ Fixed | Dashboard drift count includes fail+marginal |
| Major-4 | Major | ✅ Fixed | DriftAlertsTab shows exceeded instruments in red |
| Major-5 | Major | ✅ Fixed | Recommendations engine fetches fail instruments and surfaces each as critical |
| Major-6 | Major | ✅ Fixed | bad_actors query now requires ≥ 2 failures |
| Major-7 | Major | ✅ Fixed | Save error toast added; tenant-scope via CRITICAL-1 |
| Major-8 | Major | ✅ Fixed | CriticalityBadge amber "Unclassified" for null; form defaults to null + placeholder |
| Major-9 | Major | ✅ Fixed | Documents reads `results` not `documents` |
| Major-10 | Major | ✅ N/A | Already correct in Reports.jsx at time of audit |
| Major-11 | Major | ✅ Fixed | Explicit DocumentInstrument delete before Document delete in documents.py |
| Major-12 | Major | ⚙️ Infra | Supabase client issue; no app-code fix applicable (see notes) |
| Major-13 | Major | ✅ Fixed | AppNotFound component + catch-all Route inside /app AuthGuard layout |
| Major-14 | Major | ✅ Already Done | Mobile drawer fully implemented in Layout.jsx + Header.jsx; hamburger present |
| Minor-1 | Minor | ❌ Open | Compliance Rate shows 0.0% on empty sites |
| Minor-2 | Minor | ✅ Fixed | status=all no longer returns 500 |
| Minor-3 | Minor | ❌ Open | DELETE is soft-delete with no hint |
| Minor-4 | Minor | ❌ Open | No calibration void/correct workflow |
| Polish-1 | Polish | ❌ Open | Brand casing: Calcheq vs CalCheq |
| Polish-2 | Polish | ❌ Open | Two different `<title>` tags |
| Polish-3 | Polish | ❌ Open | Planner "+ Add" doesn't refresh queue panel |
| Polish-4 | Polish | ❌ Open | Delete confirmation sentence broken |
| Polish-5 | Polish | ✅ Fixed | Uploaded By column reads `uploaded_by` |
| Polish-6 | Polish | ✅ Fixed | "30-day" → "14-day" trial copy in billing |
| Polish-7 | Polish | ❌ Open | Trial length inconsistent across marketing |
| Polish-8 | Polish | ❌ Open | Trial-end date renders as sibling element |
| Polish-9 | Polish | ✅ Fixed | Resources cards link to /resources/:slug |

**Totals: 19 fixed · 8 open · 1 infra-only**

---

## Executive Summary

Calcheq is functional end-to-end for a single-user happy path, but it ships with three critical bugs that silently corrupt its core value proposition (compliance reporting, tenant isolation, and SEO discoverability). Fixing these three in order is the highest-leverage change before onboarding the first paying customer:

1. **Tenant isolation is broken.** A globally unique `instruments.tag_number` constraint means one customer can block another from registering a common tag like "PT-101". This also enables cross-tenant enumeration. (CRITICAL-1)
2. **Compliance numbers lie.** `instruments.last_calibration_date` is set to the *last submitted* calibration rather than the *most recent by date*. A FAIL entered today can be silently overridden by an older PASS submitted afterwards. Dashboard, Reports, Smart Diagnostics, badges — all read from this stale field. (CRITICAL-2)
3. **Every page is effectively invisible to Google.** The canonical URL on every marketing page resolves to the homepage, so the Resources blog strategy contributes zero SEO signal. (CRITICAL-3)

Below these, a cluster of major bugs share a single root cause: **API response-shape mismatch** (frontend reads `response.items` where backend returns `{total, results}`). Fixing the shape contract across Documents, Reports, and Diagnostics should knock out at least four "feature silently shows empty" bugs in one change.

The smaller polish list is long but not blocking. Brand casing, inline date rendering, and trial-copy inconsistencies are mostly 15-minute fixes that would materially lift perceived quality.

**Overall readiness:** safe for a single hand-held pilot; not safe for self-serve sign-ups or any customer who imports historical data.

---

## Bug Count by Severity

| Severity | Total | Fixed | Open |
|---|---|---|---|
| Critical | 3 | 3 | 0 |
| Major | 14 | 13 | 1 infra-only (Major-12) |
| Minor | 3 | 1 | 2 (+ Minor-4 is by-design debate) |
| Polish | 9 | 4 | 5 |

---

## Critical

### ✅ CRITICAL-1 — `tag_number` uniqueness is GLOBAL across tenants
- **Fixed:** `backend/models.py` — removed `unique=True` from `tag_number`; added `UniqueConstraint('tag_number', 'created_by', name='uq_instrument_tag_per_site')` via `__table_args__`. DB constraint swapped from `instruments_tag_number_key` (global) to `uq_instrument_tag_per_site` (composite) via MCP migration on 2026-04-21.
- **Commit:** `be896c0`

---

### ✅ CRITICAL-2 — `instrument.last_calibration_date` uses "last submitted", not "most recent by date"
- **Fixed:** `backend/routes/calibrations.py` `_update_instrument_on_approve` — guard clause skips update if `record.calibration_date < instr.last_calibration_date`. DB recompute of all existing stale values run via MCP on 2026-04-21.
- **Commit:** `be896c0`

---

### ✅ CRITICAL-3 — Every page declares the homepage as its canonical URL (SEO)
- **Fixed:** `frontend/src/App.jsx` — added `CanonicalManager` component (uses `useLocation`) that dynamically sets `<link rel="canonical">` and `<meta property="og:url">` on every route change.
- **Commit:** `be896c0`

---

## Major

### ✅ Major-1 — Sign-in step 1 "Your company" doesn't bind user to that site
- **Fixed:** `frontend/src/pages/auth/SignIn.jsx` `handleSignIn` — after Supabase `signInWithPassword` succeeds, fetches `/api/auth/me` with the new JWT. If `me.site_name` differs from the typed company name, calls `supabase.auth.signOut()`, shows an error, and resets to step 1. Network failure during the cross-check allows login to proceed (fail-open so infra blips don't lock users out).
- **Commit:** `a24696f`

### ✅ Major-2 — Dashboard fires duplicate API calls on every load
- **Fixed:** `frontend/src/pages/Dashboard.jsx` `useDashboard` — replaced `tick` counter + `caltrack-user-change` increment with a `user` state that is updated from the event's `e.detail`. The fetch effect depends on `[site, demoMode, refresh]` (derived from `user`) instead of `[tick]`. Added `if (!site) return` guard so no fetch fires before auth resolves (was the source of the first duplicate call). `retry` now increments a separate `refresh` counter.
- **Commit:** `ca7a5be`

### ✅ Major-3 — Dashboard Drift Alerts card ignores "exceeded" drift status
- **Fixed:** `frontend/src/pages/Dashboard.jsx` — `useDashboard` hook now fetches both `last_calibration_result=marginal` and `last_calibration_result=fail` instruments; `driftCount` sums both totals.
- **Commit:** `dd32745`

### ✅ Major-4 — Smart Diagnostics "Drift Alerts" tab also ignores "exceeded"
- **Fixed:** `frontend/src/pages/SmartDiagnostics.jsx` `DriftAlertsTab` — now fetches marginal + fail in parallel; exceeded instruments tagged `_driftStatus='exceeded'`, sorted to top, shown with red "Exceeded" pill and `'exceeded'` in Fail At column.
- **Commit:** `dd32745`

### ✅ Major-5 — Smart Diagnostics "Recommendations" shows "All clear" despite FAIL today + exceeded drift
- **Fixed:** `frontend/src/pages/SmartDiagnostics.jsx` `RecommendationsTab.load` — added a 5th parallel fetch for `last_calibration_result=fail, status=active` instruments. Each result is pushed to the `crit` array with criticality-aware description (safety_critical → "immediately", process_critical → "as soon as possible", other → generic). Previously the tab only checked overdue status and repeat failures; a standalone FAIL with no overdue flag was invisible to the engine.
- **Commit:** `ca7a5be`

### ✅ Major-6 — Smart Diagnostics "Repeat Failures" shows instruments with only 1 failure
- **Fixed:** `backend/routes/dashboard.py` `bad_actors` query — added `.having(func.count(CalibrationRecord.id) >= 2)` to match the UI copy "2 or more as-found failures".
- **Commit:** `be896c0`

### ✅ Major-7 — Edit instrument form silently swallows 409 on save
- **Fixed:** (a) Backend tenant-scope fixed via CRITICAL-1. (b) `frontend/src/pages/InstrumentForm.jsx` catch block now calls `showToast(err.message || 'Failed to save instrument', 'error')` so the error is visible regardless of scroll position.
- **Commit:** `19d5be5`

### ✅ Major-8 — Three different renderings of NULL criticality across the app
- **Fixed:**
  - `frontend/src/components/Badges.jsx` `CriticalityBadge` — added explicit `criticality == null` branch before the `non_critical` fallback; renders amber "Unclassified" badge instead of grey "Non-Critical".
  - `frontend/src/pages/InstrumentForm.jsx` — changed `useState('standard')` → `useState(null)` and `?? 'standard'` → `?? null` so existing instruments with null criticality are loaded correctly.
  - `CriticalitySelect` trigger — changed `?? CRITICALITY_OPTS[2]` fallback to `?? null`; when `selected` is null, renders "Select criticality…" placeholder text instead of showing the Standard pill.
- **Commit:** `ca7a5be`

### ✅ Major-9 — Documents → Upload modal "Link to Instruments" says "No active instruments found" when there are 7
- **Fixed:** `frontend/src/pages/Documents.jsx` — `useEffect` now reads `docsRes.results` and `instrsRes.results` (was `.documents` / `.instruments`).
- **Commit:** `19d5be5`

### ✅ Major-10 — Reports → Failed Report shows "0 of 0 failed calibrations" when API returns 1
- **Status:** No code change needed — `Reports.jsx` already used `res?.results ?? res ?? []` at time of fix session. Issue may have been present in an earlier build or resolved by a prior commit.

### ✅ Major-11 — DELETE /api/documents/:id returns 503 (but document is deleted)
- **Fixed:** `backend/routes/documents.py` DELETE handler — added explicit `db.query(DocumentInstrument).filter(DocumentInstrument.document_id == doc_id).delete(synchronize_session=False)` before `db.delete(doc)`. The 503 was caused by a DB foreign-key constraint violation on the `document_instruments` join table after the Document row was deleted without clearing its children first (no SQLAlchemy cascade was configured on the model). Now returns 204 cleanly.
- **Commit:** `ca7a5be`

### ⚙️ Major-12 — Supabase JWT refresh fails repeatedly in production (infrastructure)
- **Console evidence:** 9× `TypeError: Failed to fetch` from `FB._refreshAccessToken` / `FB._callRefreshToken` — all originate inside the Supabase auth client bundle, not app code.
- **Assessment:** `frontend/src/utils/userContext.js` already correctly skips re-fetching `/api/auth/me` on `TOKEN_REFRESHED` / `USER_UPDATED` events. There is no app-layer code fix applicable. The failures are either transient Supabase endpoint blips or a CSP header missing for `https://qdrgjjndwgrmmjvzzdhg.supabase.co`. Check the Supabase project health dashboard and Railway response headers for a `Content-Security-Policy` that blocks the refresh XHR.
- **No code change.**

### ✅ Major-13 — 404/unknown /app/* routes silently redirect to marketing homepage
- **Fixed:** `frontend/src/App.jsx` — added `AppNotFound` component (large "404" heading + "Go to Dashboard" CTA link) and a `<Route path="*" element={<AppNotFound />}>` catch-all inside the `/app` AuthGuard+Layout subtree, placed before the legacy redirect routes. Unknown /app/* paths now show an in-app 404 with sidebar intact instead of bouncing to the marketing homepage.
- **Commit:** `a24696f`

### ✅ Major-14 — App shell is not mobile-responsive
- **Status:** Already fully implemented — not deferred. `frontend/src/components/Layout.jsx` has: `sidebarOpen` state, a `bg-black/50 lg:hidden` backdrop div, and the sidebar wrapper using `transform -translate-x-full lg:translate-x-0` with `transition-transform`. `Header.jsx` has a hamburger `<button className="lg:hidden">` calling `onMenuClick`. `Sidebar.jsx` calls `onNavigate()` on every link click to close the drawer.
- **No code change required.**

---

## Minor

### ❌ Minor-1 — Compliance Rate card shows "0.0%" and red "Below 95% target" when there are 0 instruments
- Dashboard with zero instruments shouldn't imply a failing state.
- Fix: render "—" / "N/A" / "Add instruments to see compliance" on empty sites.

### ✅ Minor-2 — GET /api/instruments?status=all → HTTP 500
- **Fixed:** `backend/routes/instruments.py` — added `and status != 'all'` guard so `status=all` is treated as no filter rather than an invalid enum.
- **Commit:** `be896c0`

### ❌ Minor-3 — DELETE /api/instruments/:id is actually a soft-delete but returns 204 with no hint
- DELETE returns 204 empty body. GET after delete still returns 200 with `status: "decommissioned"`. Reasonable for calibration history, but undocumented and confusing to API consumers.
- Fix: return 200 with the updated record (status=decommissioned), or rename route to `POST /api/instruments/:id/decommission` and make DELETE return 405. Or add `?hard=true` for genuine hard delete.

### ❌ Minor-4 — No DELETE endpoint for calibration records (405)
- Admin has no UI path or API path to remove a wrongly-entered calibration. If records are immutable by design (audit integrity) that's fine — but provide a "correct" / "void" workflow with a mandatory note.

---

## Polish

### ❌ Polish-1 — Brand casing inconsistency: "Calcheq" vs "CalCheq"
- Tab title reads "Calcheq"; logo/nav/body copy reads "CalCheq"; sign-in page says "New to CalCheq?". Pick one and apply globally.

### ❌ Polish-2 — Two different `<title>` tags across app
- Marketing: "CalCheq — Calibration Management for Australian Processing Plants"
- App (post-navigation): "Calcheq — Instrument Calibration Management for Process Industries"
- Drops the Australian positioning on app pages. Align to a single canonical title.

### ❌ Polish-3 — Planner "+ Add" doesn't refresh queue panel — user has to click Refresh
- POST /api/queue succeeds (201). Server state is correct. UI still shows "Queue is empty" until Refresh is clicked. Fix: optimistic update or re-fetch on success.

### ❌ Polish-4 — Documents delete confirmation sentence breaks with title on new line
- Renders "Are you sure you want to delete ? This action cannot be undone." with the title below as a separate block. Fix: inline the title inside the sentence: "Are you sure you want to delete 'QA-TEST-DOC-20260420'?"

### ✅ Polish-5 — Documents list table "Uploaded By" column renders empty
- **Fixed:** `frontend/src/pages/Documents.jsx` — table cell now reads `doc.uploaded_by` (was `doc.created_by`).
- **Commit:** `19d5be5`

### ✅ Polish-6 — Billing fine print claims "30-day free trial" (inconsistent with 14-day self-serve)
- **Fixed:** `frontend/src/pages/AppSettings.jsx` — hardcoded "30-day free trial included" changed to "14-day free trial included".
- **Commit:** `072c866`

### ❌ Polish-7 — Trial length is described inconsistently across marketing + app
- Marketing site consistently says 30-day (FAQ, Contact H1 "Start Your Free 30-Day Pilot", Pricing CTA, hero CTA). CLAUDE.md says self-serve = 14 days, pilot = 30 days. Billing copy hardcoded to 30-day.
- Fix decision needed: unify on 30 days (simplest) or make self-serve copy say 14-day explicitly.

### ❌ Polish-8 — Billing panel sentence: trial-end date renders as a separate block
- "Your free trial ends on . Choose a plan below..." — the date 19 May 2026 is a sibling element, so the sentence reads broken.

### ✅ Polish-9 — Resources cards link to /blog/:slug, not canonical /resources/:slug
- **Fixed:** `frontend/src/pages/marketing/Resources.jsx` — card `to` prop changed from `/blog/${r.slug}` to `/resources/${r.slug}`.
- **Commit:** `072c866`

---

## Observations (not bugs)

- **IXOM account is empty.** Sidebar + header + dashboard all show "Calcheq" (the user's own site, which holds no real data). CLAUDE.md guidance to treat IXOM as "real pilot — do not delete" still noted; all test activity was on Calcheq site.
- **Response shape contract.** The single highest-leverage refactor below the three critical bugs is to unify the API response shape (`{total, results}`) and fix all frontend accessors in one change — that alone resolves Major-9 and Major-10 and prevents the same bug recurring.
- **Pattern: stale denormalised fields.** CRITICAL-2 is the clearest instance but the pattern (reading from `instruments.last_*` fields instead of aggregating live from `calibration_records`) shows up in Dashboard, Smart Diagnostics Recommendations, and likely Reports. Worth a dedicated audit.

---

## Remaining Open Items

All Major bugs are resolved. Remaining open items:

| ID | Description | Effort |
|----|-------------|--------|
| Major-12 | Supabase JWT refresh blips — infra/CSP, no code fix | Infrastructure |
| Minor-1 | Compliance Rate shows "0.0%" on empty sites | 30 min |
| Minor-3 | DELETE /api/instruments returns 204 with no hint it's a soft-delete | Design decision |
| Minor-4 | No calibration void/correct workflow | Design decision |
| Polish-1 | Brand casing: "Calcheq" vs "CalCheq" | 30 min find-replace |
| Polish-2 | Two different `<title>` tags across app | 30 min |
| Polish-3 | Planner "+ Add" doesn't refresh queue panel | 30 min |
| Polish-4 | Delete confirmation sentence renders title on new line | 15 min |
| Polish-7 | Trial length inconsistent across marketing (14 vs 30 days) | Content decision |
| Polish-8 | Billing panel trial-end date renders as sibling element | 15 min |
